/**
 * Created by sebby on 9/25/16.
 */
package io.digitalstream.library

object Arrays {

  fun run(): Unit {
    val xs = arrayOf(1, 2, 3)
    val ys = Array(20, { i -> 0 })
    val x = xs[0]
    val length = xs.size
  }
}
